"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const discord_js_1 = require("discord.js");
const errors_1 = require("../errors");
const FrameworkModule_1 = __importDefault(require("../FrameworkModule"));
class ButtonsModule extends FrameworkModule_1.default {
    constructor(client) {
        super(client, 'buttons');
        this.components = new discord_js_1.Collection();
        this.client.on('interactionCreate', (interaction) => __awaiter(this, void 0, void 0, function* () {
            if (!interaction.isButton())
                return;
            let json;
            try {
                json = JSON.parse(interaction.customId);
            }
            catch (error) {
                this.client.emit('error', new errors_1.FrameworkError('InvalidCustomId', interaction.customId));
            }
            if (!json.action || !this.components.has(json.action)) {
                this.emit('unknown', interaction);
                return;
            }
            const button = this.components.get(json.action);
            try {
                this.emit('run', {
                    button,
                    interaction,
                });
                yield button.run(json, interaction);
                this.emit('success', {
                    button,
                    interaction,
                });
            }
            catch (error) {
                this.emit('error', {
                    button,
                    error,
                    interaction,
                });
            }
        }));
    }
}
exports.default = ButtonsModule;
/**
 * Emitted when a button throws an error
 * @event ButtonsModule#error
 * @param {SlashCommand} context.command The command this interaction is for
 * @param {Button} context.button The button that threw an error
 * @param {ButtonInteraction} context.interaction The interaction
 * @param {Error} context.error The error
 */
/**
 * Emitted when a button is executed
 * @event ButtonsModule#run
 * @param {SlashCommand} context.command The command
 * @param {Button} context.button The button
 * @param {ButtonInteraction} context.interaction The interaction
 */
/**
 * Emitted when a button finishes execution successfully
 * @event ButtonsModule#success
 * @param {MessageCommand|SlashCommand|UserCommand} context.command The command
 * @param {Button} context.button The button
 * @param {ButtonInteraction} context.interaction The interaction
 */
/**
 * Emitted when an interaction arrives with a button that is not loaded
 * @event ButtonsModule#unknown
 * @param {ButtonInteraction} interaction The interaction
 */ 
