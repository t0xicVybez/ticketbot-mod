"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const errors_1 = require("./errors");
class FrameworkComponent {
    constructor(client, options) {
        this.client = client;
        this.filepath = options.filepath;
        if (typeof options.id !== 'string')
            throw new errors_1.FrameworkTypeError('InvalidType', 'id', 'string', typeof options.id);
        this.id = options.id;
        if (!this.client.mods.has(options.moduleName))
            throw new errors_1.FrameworkTypeError('UnknownModule', this.id, options.moduleName);
        this.mod = this.client.mods.get(options.moduleName);
    }
    /** Reload the component */
    reload() {
        return this.mod.reload(this.id);
    }
    toString() {
        return this.id;
    }
    /** Unload the component */
    unload() {
        return this.mod.unload(this.id);
    }
}
exports.default = FrameworkComponent;
