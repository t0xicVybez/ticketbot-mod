"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const discord_js_1 = require("discord.js");
const FrameworkModule_1 = __importDefault(require("../FrameworkModule"));
class AutocompleteModule extends FrameworkModule_1.default {
    constructor(client) {
        super(client, 'autocomplete');
        this.components = new discord_js_1.Collection();
        this.client.on('interactionCreate', (interaction) => __awaiter(this, void 0, void 0, function* () {
            if (interaction.type !== discord_js_1.InteractionType.ApplicationCommandAutocomplete)
                return;
            const option = interaction.options.getFocused(true);
            if (!option || !this.components.has(option.name)) {
                this.emit('unknown', interaction);
                return;
            }
            const completer = this.components.get(option.name);
            const command = this.client.commands.commands.slash.find(c => c.name === interaction.commandName);
            if (command) {
                try {
                    this.emit('run', {
                        command,
                        completer,
                        interaction,
                    });
                    yield completer.run(option.value, command, interaction);
                    this.emit('success', {
                        command,
                        completer,
                        interaction,
                    });
                }
                catch (error) {
                    this.emit('error', {
                        command,
                        completer,
                        error,
                        interaction,
                    });
                }
            }
            else {
                this.emit('unknown', interaction);
            }
        }));
    }
}
exports.default = AutocompleteModule;
/**
 * Emitted when a completer throws an error
 * @event AutocompleteModule#error
 * @param {SlashCommand} context.command The command this autocomplete interaction is for
 * @param {Autocompleter} context.completer The autocompleter that threw an error
 * @param {AutocompleteInteraction} context.interaction The autocomplete interaction
 * @param {Error} context.error The error
 */
/**
 * Emitted when a completer is executed
 * @event AutocompleteModule#run
 * @param {SlashCommand} context.command The command
 * @param {Autocompleter} context.completer The autocompleter
 * @param {AutocompleteInteraction} context.interaction The autocomplete interaction
 */
/**
 * Emitted when a completer finishes execution successfully
 * @event AutocompleteModule#success
 * @param {MessageCommand|SlashCommand|UserCommand} context.command The command
 * @param {Autocompleter} context.completer The autocompleter
 * @param {AutocompleteInteraction} context.interaction The interaction
 */
/**
 * Emitted when an interaction arrives with a completer that is not loaded
 * @event AutocompleteModule#unknown
 * @param {AutocompleteInteraction} interaction The interaction
 */ 
